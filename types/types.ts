export interface titleProps {
    title: string;
    text: string;
    classes?: string;
}

export interface cardProps {
    imgs: string[];
    title: string;
}

export interface cardPropsTwo {
    id: number;
    cardImg: string;
    view: string;
    duration: string;
}
